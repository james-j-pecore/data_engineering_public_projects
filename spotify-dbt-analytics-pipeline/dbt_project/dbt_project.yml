name: spotify_analytics
version: "1.0.0"
config-version: 2

profile: spotify_analytics

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  spotify_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
