name: nyc_electricity_analytics
version: "1.0.0"
config-version: 2

profile: nyc_electricity_analytics

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  nyc_electricity_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
